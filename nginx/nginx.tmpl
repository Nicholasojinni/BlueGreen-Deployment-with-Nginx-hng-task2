# nginx.tmpl - placeholder template file (start.sh will generate final config).
# This file is kept for transparency and to satisfy mount expectations.
# The start.sh entrypoint generates /etc/nginx/conf.d/default.conf based on ACTIVE_POOL.
